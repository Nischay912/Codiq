Now : lets push the code to github > so we can create a repository on github > then in terminal go in root i.e. outside all backend frontend if any and type > "git init" > then "git remote add origin https://github.com/Nischay912/Codiq.git" > then "git add ." > git commit -m "codebase setup done and implemented" > "git push -u origin master"