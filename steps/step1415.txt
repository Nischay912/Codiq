Now here : when the HOST clicks on END SESSION there > and then : he will be redirected to dashboard ; and then the participant will be redirected automatically to the dashboard too ; this 5 seconds gap is there > because : in useSessions.js > we have refetchInterval: 5000 there > so it fetches the new session every 5 seconds there and know the status changes there > and once the session is ended then the code we wrote in SessionPage.jsx in the "useEffect" that if status is completed navigate to the dashboard page : so thats what happens here ; so navigates by rule to the "dashboard" page > so after 5 seconds the session is marked completed for the participant as well and so he also gets disconnected there automatically after 5 seconds, thus here below.

So, thus once host ends call, it will refetch after 5 seconds for status of session ; and since it was made completed by host, refetch will run after 5 seconds and so the useEffect will run as refetch re-renders the whole page and all components, and there we have to navigate to dashboard if status completed, so participant will automatically navigate to dashboard page there, thus here below.

Using refetchInterval there makes it : running every 5 seconds ONLY while that page/component is open and active i.e. don't think that the refetch runs every 5 seconds in whole app ; but when we are on that page for which it was written , there it keeps refetching and calling api every 5 seconds and thus when refetches again and then useEffect sees status changed, useEffect runs and navigates participant to dashboard after 5 seconds of host ended the session, as refetch happened 5 seconds after that for the participant there, thus here below.

See the next steps in step1416.txt file now there, thus here below.