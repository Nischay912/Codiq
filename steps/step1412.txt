Now : we have that > if we are trying to leave a session by clicking the END CALL there > then : we see its joining but 3 participants become there i.e. it rejoins twice there and 3 participants gets visible there.

SO > go in "stream.js" file of frontend > then just before the API key check there > we put that : if we already have a client then : we > before creating a new client there, first check if one already exists and if it is ; then first properly disconnect the previous one then proceed ahead to create a new one ;in this way every time user rejoins , the previous instance created for the client gets disconnected , then only code moves ahead and a frehs instance is there, thus here below.

So there in that stream.js file -

We now first put there -

if(client){
    await disconnectStreamClient();
}

; so that > everytime a user joins/rejoins , the initialize function runs > so there : only after this check is passed, then only go ahead and create new client instance for the call ; thats why we put it there before the "new StreamVideoClient" function there -

So that : before creating the new check by the backend and all code if the client object for same user exists in the same session, if yes then first disconnect it using the check above and then create new instance again when rejoins using "new StreamVideoClient" there written ahead, thus here below.

So : do that in next steps there in stream.js file of frontend now here/there, thus here below.