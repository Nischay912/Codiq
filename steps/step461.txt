Now : first lets install packages > cd frontend > "npm i canvas-confetti@1.9.3 react-resizable-panels@3.0.6" > we will see later where they will be used in the app later, thus here below.

See the next steps in ProblemPage.jsx file now there, thus here below.